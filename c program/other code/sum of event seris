#include <stdio.h>
int main()
{
    int i,N,sum=0;
    printf("Enter a value:");
    scanf("%d",&N);
    for ( i=2; i<=N; i=i+2)
    {
        sum=sum+i;
    }
    printf("%d",sum);
    return 0;
    
}